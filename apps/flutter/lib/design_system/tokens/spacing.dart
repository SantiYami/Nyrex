// SPDX-License-Identifier: CC-BY-NC-SA-4.0
// Copyright (c) 2026 SantiYami

/// Spacing tokens for Nyrex.
///
/// 4px grid. All padding, margins, and gaps are multiples of 4px.
library;

// ---------------------------------------------------------------------------
// Spacing scale
// ---------------------------------------------------------------------------

/// 4px — tight internal gaps.
const double kSpaceXs = 4;

/// 8px — internal component padding.
const double kSpaceSm = 8;

/// 12px — medium internal padding.
const double kSpaceMd = 12;

/// 16px — section gaps, gutter.
const double kSpaceLg = 16;

/// 20px — large separation.
const double kSpaceXl = 20;

/// 24px — viewport margin, major sections.
const double kSpace2xl = 24;

/// 32px — page-level spacing.
const double kSpace3xl = 32;

/// 48px — hero spacing.
const double kSpace4xl = 48;

// ---------------------------------------------------------------------------
// Layout constants
// ---------------------------------------------------------------------------

/// Grid gutter width.
const double kGutter = 16;

/// Viewport margin.
const double kMargin = 24;

/// Icon rail width (JSX: 52px).
const double kRailWidth = 52;

/// Sidebar width (JSX: --side-w: 200px).
const double kSidebarWidth = 200;

/// Standard row height for nav items, list entries.
const double kRowHeight = 36;

/// Card internal padding.
const double kCardPadding = 20;
